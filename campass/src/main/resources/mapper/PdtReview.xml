<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campass.demo.dao.PdtReviewDao">
  <!-- 리뷰 목록페이지 -->
  <select id="ReviewList" parameterType="int" resultType="com.campass.demo.dto.PdtReviewDto">
  	select * from pdtReview where pCode=#{pCode}
  </select>
  
  <!-- 저장 -->
  <insert id="saveReview" parameterType="com.campass.demo.dto.PdtReviewDto">
  	insert into pdtReview values(pdtReview_seq.nextval,#{pReviewNo},#{pOrderNo},#{bId},
  	#{pStar},#{pReviewContent},#{pCode},default)
  </insert>
  
  <!-- 삭제 -->
  <delete id="deleteReview">
  	delete from pdtReview where bId=#{bId}
  </delete>
  
  <delete id="deleteReview2" parameterType="map">
  	delete from pdtReview where pReviewNo=#{pReviewNo} and bId=#{bId}
  </delete>
</mapper>
